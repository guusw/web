<?php
    // Server hostname
    $server_name = "SERVER_NAME";
    $server_http = HTTP_PORT;
    $server_https = HTTPS_PORT;
    $server_public_http = HTTP_PUBLIC_PORT;
    $server_public_https = HTTPS_PUBLIC_PORT;

    // Database settings
    $db_host = "127.0.0.1";
    $db_port = "MYSQL_PORT";
    $db_user = "MYSQL_USER";
    $db_pw = "MYSQL_PASSWORD";
    $db_name = "MYSQL_DATABASE_NAME";

    // Subdomain for uploaded data
    $data_dir = "TDRZ_DATA_DIR";

    // Subdomain for data files
    $data_sub = "TDRZ_DATA_SUBDOMAIN";
?>
