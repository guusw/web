<?php
    // Server hostname
    $server_name = "SERVER_NAME";
    $server_http = HTTP_PORT;
    $server_https = HTTPS_PORT;

    // Database settings
    $db_host = "127.0.0.1:MYSQL_PORT";
    $db_user = "MYSQL_USER";
    $db_pw = "MYSQL_PASSWORD";
    $db_name = "MYSQL_DATABASE_NAME";
    
    // Folder where site uploaded data is stored
    $data_dir = "Data";
?>
